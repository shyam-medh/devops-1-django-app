apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "django-backend.fullname" . }}-test-connection"
  labels:
    {{- include "django-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "django-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
